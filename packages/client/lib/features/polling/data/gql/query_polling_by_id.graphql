# import '/data/gql/polling_model.graphql'

query Polling($id: String!) {
  polling_by_pk(id: $id) {
    ...PollingModel
  }
  polling_variant(where: {polling_id: {_eq: $id}}) {
    id
    description
  }
}
