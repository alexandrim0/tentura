mutation BeaconCreate(
  $title: String!,
  $context: String = "",
  $description: String = "",
  $coordinates: Coordinates = {},
  $image: Upload = {},
  $startAt: String,
  $endAt: String
) {
  beaconCreate(
    title: $title,
    description: $description,
    coordinates: $coordinates,
    dateRange: $dateRange,
    context: $context,
    startAt: $startAt,
    endAt: $endAt,
    image: $image
  ) {
    id
  }
}
