fragment PollingModel on polling {
  id
  author_id
  question
  enabled
  created_at
  updated_at
  variants {
    id
    description
  }
  polling_acts {
    polling_variant_id
    created_at
  }
}
